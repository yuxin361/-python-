# 小说拆解助手 - 运行流程

## 📋 前置条件

- Python 3.9+ 已安装
- PyCharm 已安装
- 项目文件夹已复制到本地

---

## 🔧 步骤一：安装依赖

### 方法1：使用 pip 安装（推荐）

打开命令提示符（CMD）或终端，进入项目目录：

```bash
cd c:\Users\16121\Desktop\novel\novel_unpack_final
```

执行安装命令：

```bash
pip install -r requirements.txt
```

### 方法2：手动安装

如果上述命令失败，可以手动安装：

```bash
pip install requests>=2.28.0
pip install python-dotenv>=1.0.0
```

---

## 🔑 步骤二：配置 API

### 方式1：修改 .env 文件（推荐）

1. 打开项目文件夹中的 `.env` 文件
2. 在 `DASHSCOPE_API_KEY=` 后面填写您的阿里云 API Key

```
# .env 文件内容
DASHSCOPE_API_KEY=sk-您的API密钥

# Bing 搜索 API（可选）
# BING_SEARCH_API_KEY=您的Bing密钥
```

### 方式2：在 GUI 界面配置

1. 运行项目后，在左侧配置面板中直接输入：
   - **大模型 API Key**：您的阿里云 API Key
   - **大模型名称**：默认 `qwen-turbo`（保持不变）
   - **API Base**：默认 `https://dashscope.aliyuncs.com/compatible-mode/v1`（保持不变）
   - **Bing API Key**（可选）：用于真实搜索

---

## 🚀 步骤三：运行项目

### 方法1：使用 PyCharm（推荐）

1. 打开 PyCharm
2. 选择 `File` → `Open`，打开项目文件夹
3. 在项目结构中找到 `main.py`
4. 右键点击 `main.py` → `Run 'main'`

### 方法2：使用命令行

```bash
cd c:\Users\16121\Desktop\novel\novel_unpack_final
python main.py
```

---

## 🔍 步骤四：使用流程

1. **输入小说名称**：在右下角输入框中输入要拆解的小说名称（如「三体」）
2. **点击搜索**：点击「搜索书籍」按钮
3. **选择书籍**：点击搜索结果中的书籍卡片
4. **开始拆解**：点击「开始拆解」按钮
5. **查看结果**：拆解完成后，报告保存在 `output` 文件夹中

---

## 📁 项目结构说明

```
novel_unpack_final/
├── .env              # API 配置文件
├── main.py           # 主程序入口
├── requirements.txt  # 依赖列表
├── output/           # 拆解报告输出目录
├── prompts/          # 提示词模板目录
└── src/              # 源代码目录
    ├── simple_gui.py # 主界面
    ├── llm.py        # 大模型客户端
    ├── search.py     # 搜索服务
    └── ...
```

---

## ⚠️ 注意事项

1. **API Key 获取**：阿里云 API Key 需要从阿里云百炼控制台获取
2. **网络连接**：运行时需要保持网络连接
3. **模拟模式**：如果没有 API Key，可以勾选「模拟模式」体验功能（仅显示模拟数据）
4. **权限问题**：确保项目文件夹有读写权限

---

## 🔗 API 获取地址

| 服务 | 获取地址 |
|------|----------|
| 阿里云通义千问 | https://dashscope.aliyuncs.com/ |
| Bing 搜索 API | https://azure.microsoft.com/zh-cn/products/cognitive-services/bing-web-search-api/ |
